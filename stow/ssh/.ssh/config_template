# This is a template file. Copy it to `~/.ssh/config` and edit. Also, don't
# forget to run `chmod 600 config` or it might not work.

# Fix idle connections dropping.
ServerAliveInterval 30

# Template for host configuration.
#Host <alias>
#    HostName <host-addr>
#    User <username>
